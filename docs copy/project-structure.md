> **Keep this file updated after any code change in this area — do not let it drift from actual implementation.**

# Project Structure

**Ch'rps** is a Next.js 14 App Router application written in TypeScript, styled with Tailwind CSS, and deployed on Vercel — Next.js API routes act as the backend (serverless functions), with MongoDB (via Mongoose) as the persistence layer. There is no separate backend service; `app/api/**/route.ts` files are the entire API surface. A native iOS shell (Capacitor, server-URL mode — see "iOS Native Shell" below) wraps this same deployed web app; it does not change or duplicate any of the above.

Ch'rps is a multi-tenant, restaurant-style shift-check app — structured checklist tasks (`form`-type: numeric readings, yes/no fields, or checklist sub-items) organized into task lists (Opening/Mid-Shift/Closing/manager-created custom lists) plus one or more standalone anytime task lists. Every company using Ch'rps is a `Company` tenant, and a manager can create/rename/delete task lists and set their day-of-week schedule — see CLAUDE.md's Multi-Tenancy section and `docs/features/task-lists.md`. It's a lean fork of an earlier personal habit/routine tracker; the personal-habit framing (timer-based Countdown/Stopwatch/Checkbox item types as the primary type, the Sunday "Routine Review" time-variance feature) and an even earlier virtue/goals/quotes layer ("A Good Man") have both been retired, and the product vocabulary itself was later renamed from "Routine"/"Habit" to "TaskList"/"Task" throughout the codebase and docs — see `CLAUDE.md` for the current product brief and its Vocabulary note. There used to be a **native iOS App Intents/Shortcuts layer** (`ios/App/App/AppIntents/`, `ios/App/App/ChrpsAPI.swift`) carrying pre-pivot "Habit"/"Routine" naming — deleted outright rather than renamed, since Shortcuts integration was never load-bearing for this app; see "iOS Native Shell" below for the removal note. The `RoutineActivity` Xcode target/Widget Extension (the Live Activity) still carries the same pre-pivot naming and is *not* going anywhere — that one's a deliberate, documented exception, not an oversight.

## Folder Map

| Folder | What lives here |
|---|---|
| `app/` | Next.js App Router: pages/layouts under `app/(app)/` and the top-level routes, plus every API route handler under `app/api/**/route.ts`. |
| `components/` | Shared React client components — page-level views (`TasksView.tsx`, `ReportsView.tsx`/`ReportsContent.tsx` plus the `reports/` subfolder (`ManagerOverview.tsx`, `EmployeeOverview.tsx`, `LogsTab.tsx`, `TaskListChart.tsx`, `TaskStatRow.tsx`, `shared.ts`) — see reports.md, `ManageTasksView.tsx`, …), bottom-sheet/modal flows (`AddTaskSheet.tsx`, `AddTaskListSheet.tsx`, `FABTodoSheet.tsx`, `EditTodoSheet.tsx`, …), list/row presentational pieces (`TaskListCard.tsx`, `TaskRow.tsx`, `TaskCard.tsx`, `AppIcon.tsx`, `StreakDots.tsx`, `TimelineBar.tsx`, …), the task-creation field editor (`TaskFieldsEditor.tsx`, shared by `AddTaskSheet.tsx` and `TaskListEditView.tsx`), the NFC tap-to-trigger UI (`NfcClaimTagPicker.tsx`, `NfcDoneScreen.tsx`, `NfcTagLinkedSetup.tsx`, `UniversalLinkHandler.tsx` — see nfc.md), `NoCompanyMessage.tsx` (shown when a signed-in user's `companyId` is still `null`), `NativeBootstrap.tsx` (Live Activity push-token forwarding on native cold start — used to also push the API key into Keychain for App Intents, removed, see "iOS Native Shell" below), and the offline online/offline context + status banner (`NetworkStatusProvider.tsx`, `OfflineBanner.tsx` — see offline.md). |
| `lib/` | DB connection utilities (`mongoose.ts`, `mongodb-client.ts`), auth config (`auth.ts`, `auth.config.ts`), session/company/role resolution (`session.ts` — see CLAUDE.md's Multi-Tenancy section), idempotent seed/bootstrap logic (`seed.ts`, `seed-templates.ts`), form-field validation shared by the write routes (`form-fields.ts`, see task-lists-api.md), the `Task`/`TaskDefinition` catalog join and NFC binding (`task-definitions.ts` — `resolveTasks`/`resolveTask`/`resolveMostRelevantPlacement`/`bindNfcTag`/`unbindNfcTag`, see task-lists.md and nfc.md), the pure placement-selection logic shared by the online and offline NFC-resolution paths (`placement-resolution.ts` — `pickMostRelevantPlacement`, deliberately dependency-free so client code can import it — see offline.md), the offline SQLite cache/outbox (`offline-db.ts`, `offline-sync.ts`, `offline-nfc-resolver.ts` — see offline.md), and small shared helpers (`task-visibility.ts` — the day-of-week visibility gate, see task-lists.md — `task-progress.ts` — the weekly dot/pacing math shared by `StreakDots` and Reports — `streak.ts` — the employee "current streak" computation, see reports.md — `task-log-actions.ts`/`task-list-session-actions.ts`/`task-transition.ts` — the single-active-timer sweep/pause logic and `TaskListSession` bookkeeping shared by the app's own task-logs routes and the NFC Universal Link trigger, see task-lists-api.md and timer.md — `task-log-events.ts` — the client-side `TASK_LOG_CHANGED_EVENT` the FAB and `TasksView`'s today list both listen for, alongside their own short-interval polling, see timer.md — `useRingDrag.ts`, `projected-finish.ts`, `task-timeline.ts` — the timer ring's drag-to-set-time gesture and the live projected-finish/timeline math, see timer.md — `week-dates.ts`, `useTodoActions.ts` — `task-trigger.ts` — `triggerTask()`, the shared start/complete case dispatch now called only by the NFC Universal Link page (`app/nfc/[tagCode]/page.tsx`) as a plain library function, see nfc.md — `apns.ts` — signs and sends Live Activity push updates directly to Apple's push servers, see live-activity.md — `native/` — thin Capacitor plugin bridges (`live-activity-bridge.ts`, `nfc-scan-bridge.ts`/`nfc-scan.ts` — see nfc.md, `routine-activity.ts` — this one and `live-activity-bridge.ts` deliberately keep their "Routine"-era names to match the un-renamed iOS RoutineActivity target, see the note in `lib/native/routine-activity.ts`), see nfc.md and live-activity.md. `api-key.ts` and `native/api-key-bridge.ts` used to live here too — deleted along with the rest of the external API, see "iOS Native Shell" below). |
| `models/` | Mongoose schema/model definitions, one per MongoDB collection (`Company.ts`, `User.ts` — `companyId`/`role`, see CLAUDE.md — `TaskTemplate.ts`, `TaskList.ts`, `Task.ts` (a list placement) + `TaskDefinition.ts` (the catalog entry it points at — name/icon/`taskType`/`formFields`/`nfcTagUid`, also exports the shared `FormFieldDef`/`FormFieldDefSchema` used by `TaskTemplate` too — see task-lists.md's "Company Task Catalog" split), `TaskLog.ts`, `TaskListSession.ts`, `Todo.ts`, `NfcTag.ts`/`PendingNfcLink.ts` — see nfc.md). `AppIntentLink.ts` used to live here too, tracking Shortcuts usage per task — deleted along with the rest of the App Intents layer, see "iOS Native Shell" below. |
| `public/` | Static assets served at the web root — app icons, the PWA `manifest.json`, and the service worker `sw.js`. |
| `scripts/` | One-off, manually-run scripts — not wired into app boot. Currently just `_tmp-check-dev-user.mjs` (scratch debug script, not part of any workflow). |
| `types/` | Shared/ambient TypeScript declarations (currently `next-auth.d.ts`, extending the NextAuth session/user types). |
| `ios/` | Generated Xcode project for the native shell (Capacitor). `ios/App/App.xcodeproj` is the project to open — this Capacitor version uses Swift Package Manager, not CocoaPods, so there is no `.xcworkspace`. `ios/App/App/MainViewController.swift` registers native plugins (`capacitorDidLoad`) and disables WebView scroll bounce — used to also register an App Intents Keychain bridge plugin here, removed along with `ios/App/App/AppIntents/` (see "iOS Native Shell" below). `ios/DerivedData/` is Xcode build cache, gitignored. |
| `assets/` | Master 1024×1024 icon and splash source images (generated from the jackalope logo via `npx capacitor-assets generate`), consumed to produce `ios/App/App/Assets.xcassets/`. |
| `capacitor.config.ts` | Capacitor config at repo root — `server.url` points the native WebView at the deployed Vercel URL (server-URL mode: no static export, the native shell just loads the live site). `server.allowNavigation` allowlists `accounts.google.com` so Google OAuth completes inside the app's own WebView instead of Capacitor bouncing it out to system Safari, which would break Auth.js's CSRF/session cookie continuity — see "iOS Native Shell" below. |

## iOS Native Shell

The iOS app is a Capacitor wrapper in **server-URL mode**: the native `WKWebView` loads the live production Next.js deployment directly (`capacitor.config.ts`'s `server.url`), the same as a browser tab, just without browser chrome and with its own icon/splash. There is no static export and no bundled frontend — the Next.js codebase requires zero changes for this to work, and any future edits to `app/`/`components/` ship to the iOS app automatically the next time production redeploys (no App Store review needed for content changes, only for native shell changes).

### App Intents/Shortcuts and the external API — removed entirely

`ios/App/App/AppIntents/` (`TriggerHabitIntent.swift`, `HabitEntity.swift`, `HabitEntityQuery.swift`, `ChrpsShortcuts.swift`) and `ios/App/App/ChrpsAPI.swift` used to hold this app's native Siri/Shortcuts integration — the "Trigger Habit" App Intent, appearing in Shortcuts/Siri/Spotlight automatically, backed by a live task picker and a Keychain-based API key. **Deleted outright**, along with everything it depended on or was depended on by:

- **Native Swift**: `ios/App/App/AppIntents/` (all four files), `ChrpsAPI.swift`, `ApiKeyBridgePlugin.swift`, `KeychainHelper.swift` — the last two because nothing else read/wrote the Keychain-stored API key once the App Intents were gone (the Live Activity's own old use of Keychain was already removed separately, see [`live-activity.md`](features/live-activity.md#open-app-button)). Removing traditionally-managed (non-filesystem-synchronized) Xcode files needs `project.pbxproj` surgery via the `xcodeproj` Ruby gem (`Xcodeproj::Project`, `file_ref.remove_from_project`), not just deleting them from disk — the `AppIntents` `PBXGroup` and every file's `PBXBuildFile`/Sources-phase entry had to go too, or the project would reference missing files. `App.entitlements`/`RoutineActivity.entitlements`'s `keychain-access-groups` were also removed — nothing needs Keychain sharing between the App and RoutineActivityExtension targets anymore. `MainViewController.swift`'s `capacitorDidLoad()` no longer registers `ApiKeyBridgePlugin`.
- **Backend**: the entire `app/api/external/` route group (`trigger-task`, `start-timer`, `complete-active-task`, `tasks`, `nfc/[tagCode]`), `app/api/user/api-key/`, `lib/api-key.ts`, `models/AppIntentLink.ts`, `POST /api/nfc-tags/generate` (its only purpose was minting a tag reachable solely through the now-deleted `GET /api/external/nfc/[tagCode]`), `lib/task-trigger.ts`'s `completeActiveTask()` (its only caller was the deleted `complete-active-task` route), `middleware.ts`'s `api/external` matcher exclusion, and `User.apiKey`/`User`'s unique-sparse index on it (schema field removed; a lingering index on the live MongoDB collection from before this change is harmless dead weight, not cleaned up automatically — drop it by hand if it matters).
- **Web UI**: the Profile page's "External API Key" card and its fetch/copy/Keychain-push logic, `components/NativeBootstrap.tsx`'s API-key-to-Keychain push, `lib/native/api-key-bridge.ts`, `components/NfcTagLinkedSetup.tsx`'s Shortcut-building instructions (now a plain confirmation), and Manage Task List's "Siri & Shortcuts" connection status / raw-`_id` displays (both existed solely for this surface).
- **Docs**: `docs/features/app-intents.md` and `docs/api/external-api.md` deleted entirely; `docs/features/nfc.md` (its former second trigger path — the Shortcuts silent-trigger flow — and `triggerTask()`'s case-dispatch explanation, preserved there) and every doc cross-referencing either deleted file were updated in the same pass.

**Why removed, not just deprecated in place**: `TriggerHabitIntent`/the external `trigger-task`/`start-timer` endpoints and the Live Activity's old "Done" button shared the identical gap against the current `form`-task-based data model — neither could collect a form task's field answers or supply a verified NFC scan, so a blind trigger/complete either silently recorded a check with no data captured or failed outright with no way to show why (see [`nfc.md`](features/nfc.md)'s `assertNfcVerified` note and its `triggerTask()` case-dispatch section). The Live Activity side was fixed by replacing its button with one that opens the app instead (see [`live-activity.md`](features/live-activity.md#open-app-button)). Shortcuts integration was judged not load-bearing for this app going forward, so rather than build an equivalent app-opening redirect for it too, the whole surface was removed.

**One remaining capability loss worth knowing about**: opening a guided Task List Session for an *anytime* task list (no `startTime`, no in-app "Start Tasks" button by design) used to be reachable only through the external API's `routineGroupId` param — see [`anytime-tasks.md`](features/anytime-tasks.md). That's now genuinely impossible, not just undocumented; accepted as fine since nothing in practice ever called it that way.

**What's still there and still uses pre-pivot naming, deliberately**: the `RoutineActivity` Xcode target/Widget Extension (`ios/App/RoutineActivity/` — the Live Activity) still carries "Routine"/"Habit"-era naming (`RoutineActivityAttributes`, etc.) — a full native/Xcode-target rename is separate, higher-risk work that needs Xcode itself to verify, not something safely doable as a text-only pass — see CLAUDE.md's Vocabulary note. This is unrelated to and unaffected by the App Intents removal above; the Live Activity itself is very much still a live, maintained feature.

`ChrpsAPI.swift`/`ChrpsShortcuts.swift`'s Ch'rps-rebrand naming (previously `BeOneAPI.swift`/`BeOneShortcuts.swift`, the "Be One" name Siri/Shortcuts used to show) is now moot — both files are deleted, so there's no lingering "Be One" naming debt left to track either.

**Google OAuth inside the WebView:** Capacitor's default behavior sends navigation to domains outside the app's own origin (like `accounts.google.com`) out to the system Safari app. That breaks Auth.js's sign-in flow, because the CSRF token and session cookies set when the flow starts live in the app's WebView cookie jar, not Safari's — the callback lands back in Safari's separate jar and Auth.js can't validate it, surfacing as a generic `/api/auth/error` "Server error" page. The fix was to allowlist `accounts.google.com` in `capacitor.config.ts`'s `server.allowNavigation`, keeping the whole OAuth round-trip inside the app's own WebView and its single cookie jar. This did **not** require routing through `@capacitor/browser`/`ASWebAuthenticationSession` or Universal Links — the simpler in-WebView allowlist was sufficient once tested against the real production OAuth flow in the iOS Simulator.

**Branding:** `assets/icon.png` and `assets/splash.png`/`splash-dark.png` are generated once from the jackalope logo (parchment `#e8e0cc` background for the icon so the black line art is visible; the splash screen recolors the mark to parchment against the app's dark background) and then expanded into the full `Assets.xcassets` icon/splash set via `npx capacitor-assets generate --ios`. Re-run that command after editing the `assets/` masters, then `npx cap sync ios`.

**White flash between splash and content:** `WKWebView` defaults to a white canvas until the remote page's own CSS paints — a real, visible gap in server-URL mode since content loads over the network rather than instantly from a bundle. Fixed via `capacitor.config.ts`'s `ios.backgroundColor` (Capacitor's official config for the WebView's own background, applied before any content loads) plus pinning the same color on `ios/App/App/Base.lproj/LaunchScreen.storyboard`'s root view as a second guard against letterboxing. Verified by rapid-fire screenshotting through a cold launch, not just eyeballing it once.

**Native bootstrap:** `components/NativeBootstrap.tsx` (mounted in `app/layout.tsx`, `Capacitor.isNativePlatform()`-guarded) registers Live Activity push-token forwarding on every native cold start (see live-activity.md) — it used to also push the user's API key into Keychain here via a custom `ApiKeyBridgePlugin`, the first custom native plugin in this project; removed along with the rest of the App Intents layer above. Two other custom Swift plugins remain: `NfcScanPlugin.swift` (in-app tag reading, see nfc.md) and `LiveActivityPlugin.swift` (Lock Screen timer control, see live-activity.md) — `KeychainHelper.swift`, which those two never needed (only the now-deleted `ApiKeyBridgePlugin`/App Intents code read it), is gone too. Two more native capabilities have since been added as official/community Capacitor plugins rather than custom Swift (no `MainViewController.swift` registration needed): `@capacitor-community/sqlite` (the offline local cache/outbox) and `@capacitor/network` (connectivity detection) — see offline.md.

### `app/` subfolders

| Path | Purpose |
|---|---|
| `app/(app)/` | The authenticated app shell — layout with the bottom nav, plus the `tasks` (including `tasks/[taskListId]/edit` and `tasks/manage`, the Manage Tasks & Task Lists screen), `reports` (renamed from `analytics` — see reports.md), `profile`, and `store` (placeholder "Coming Soon" page, not linked from the bottom nav) pages. |
| `app/api/` | Every backend endpoint, grouped by domain (`task-lists`, `tasks`, `task-definitions`, `task-logs` (including `task-logs/history`, the Reports Logs tab's paginated date-range query — see reports.md), `task-templates`, `todos`, `reports` (renamed from `analytics`), `user`, `live-activity`, `nfc-tags`, `auth`, `seed`, `dev`). Used to also have an `external` group — API-key-authenticated (no session), for outside callers like the native App Intents "Trigger Habit" action — deleted entirely, see "iOS Native Shell" above. |
| `app/login/` | The one public, unauthenticated page (per `middleware.ts`) — Google sign-in. Honors a `?callbackUrl=` search param as the post-sign-in redirect, falling back to `/welcome`. |
| `app/welcome/` | Post-login splash screen, shown once per sign-in before landing on `/tasks`. |
| `app/nfc/[tagCode]/` | Universal Link landing page for a tapped physical NFC tag — claims/relinks/triggers depending on state, native-app-only (opens in the WebView via Associated Domains, not a browser). See nfc.md. |

## Feature Docs

| Doc | Covers |
|---|---|
| [`features/task-lists.md`](features/task-lists.md) | Shift-based task lists (Opening/Mid-Shift/Closing/Custom), task states, back-entry, streaks, the per-task weekly schedule/success-threshold model, day-of-week visibility, manager-created/renamed/deleted task lists and list-level scheduling, and the "Start Tasks" sequential session. |
| [`features/anytime-tasks.md`](features/anytime-tasks.md) | Standalone (never-collapsing) anytime task lists, quick-log flow, the task-template catalog, and where a task's schedule/threshold gets edited. |
| [`features/timer.md`](features/timer.md) | Both timer UIs (single-task and sequential-session), how elapsed time is tracked, the drag-to-set-time ring gesture, the pause/resume-on-jump single-active-timer model, the live projected-finish-time/task-list-timeline displays, and the resume-on-reload behavior. |
| [`features/reports.md`](features/reports.md) | The Reports tab (renamed from Analytics): the manager/employee role split, the 7-day fixed-calendar-week / 30-day rolling Overview dashboard, `/api/reports`'s aggregation, the schedule-aware Task Breakdown (segmented bar + pacing verdict), the employee "current streak" computation, and the new Logs tab's paginated history. |
| [`features/todos.md`](features/todos.md) | The standalone `Todo` list — no separate API doc, folded in here — overdue carry-forward on the Tasks page and the `FABTodoSheet` creation flow. |
| [`features/nfc.md`](features/nfc.md) | The two independent NFC systems: tap-to-trigger (`NfcTag`/`PendingNfcLink` models, Universal Links only — the former Shortcuts silent-trigger path is documented there as removed) and in-app scan-to-complete task binding (`TaskDefinition.nfcTagUid`, read via `NfcScanPlugin.swift`). Also documents the FAB's "scan to open" shortcut. |
| [`features/offline.md`](features/offline.md) | The native SQLite cache + mutation queue (`@capacitor-community/sqlite`) that lets task lists load and task-log mutations (including NFC scan-to-complete) work while offline, syncing once WiFi returns. Also documents a known gap: a cold app launch/full reload while offline still fails (server-URL Capacitor mode). |

## API Docs

| Doc | Covers |
|---|---|
| [`api/task-lists-api.md`](api/task-lists-api.md) | Task lists (including manager-only create/rename/delete and list-level `scheduledDays` push-down), tasks and task definitions — the `Task`/`TaskDefinition` placement/catalog split (including the `scheduledDays`/`successThreshold` schedule model, `form` tasks' `formFields`, and the definition-addressed `/api/task-definitions*` NFC-binding routes), task logs (including the `paused` state, `pausedSeconds`, the FAB's `GET /api/task-logs/active`, and a form task's captured `formData`), and task list sessions — `/api/task-lists*`, `/api/tasks*`, `/api/task-definitions*`, `/api/task-logs*`. |
| [`api/task-templates-api.md`](api/task-templates-api.md) | The task-template catalog — `/api/task-templates`. |

Todos has no separate API doc — its small surface (`/api/todos*`) is documented inline in [`features/todos.md`](features/todos.md), the same way `/api/reports` and `/api/task-logs/history` are folded into `features/reports.md` rather than split out. Live Activity's push-token endpoint (`/api/live-activity/push-token`) is documented inline in [`features/live-activity.md`](features/live-activity.md) rather than split out, same reasoning. The `/api/nfc-tags*` routes (arm, claim, generate, unlink) have no separate API doc either — they're documented inline in [`features/nfc.md`](features/nfc.md), alongside `/api/tasks/[id]/nfc-tag` and `/api/task-definitions/[id]/nfc-tag`, which task-lists-api.md documents by signature but defers to nfc.md for the feature-level flow.

## Secrets Policy

**This file and every file it links to must never contain secret values, API keys, or connection strings.** Reference environment variable *names* only, never their actual values:

- `MONGODB_URI`
- `AUTH_SECRET`
- `AUTH_GOOGLE_ID`
- `AUTH_GOOGLE_SECRET`
- `SKIP_AUTH` (local-dev-only auth bypass — never set in production)
- `APNS_KEY_ID`, `APNS_TEAM_ID`, `APNS_PRIVATE_KEY` (Live Activity push, see `lib/apns.ts` and `features/live-activity.md`)

`.gitignore` excludes `.env*.local` (covers `.env.local`, `.env.development.local`, etc.), but **not** a bare `.env` or `.env.production` — if either of those is ever added, extend `.gitignore` before committing.
